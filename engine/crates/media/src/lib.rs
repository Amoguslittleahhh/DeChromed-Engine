//! Roadmap phase: Track D5 (images) through D7 (WebRTC). Per ROADMAP.md,
//! codecs always bind existing audited decoders (never hand-rolled) -- this
//! crate will end up mostly glue code around those, plus the `<video>`/
//! `<audio>`/MSE element behavior that *is* genuinely ours to implement.

// Intentionally empty until D5 starts -- nothing to bind to yet.
